# Overview
Avoid documentation. Use help with scripts so user can find how to use functions.

## Environment
In order of best to worst choose python with virtual environments then docker compose then natively installing software. Use requirements.txt and similar methods to track the 

## Preferred programming languages
C++ then C then python then haskell then others. But pick what is best for the job.
